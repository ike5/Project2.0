# Shared secret: the Redis password. Consumed by BOTH redis (to require auth) and
# guestbook (to authenticate). Demo value — never commit real secrets.
apiVersion: v1
kind: Secret
metadata:
  name: redis-auth
  namespace: guestbook
type: Opaque
stringData:
  REDIS_PASSWORD: "capstone-demo-password"
