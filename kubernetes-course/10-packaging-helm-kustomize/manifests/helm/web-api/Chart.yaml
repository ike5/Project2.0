apiVersion: v2
name: web-api
description: The course sample web-api, packaged as a Helm chart
type: application
version: 0.1.0          # chart version
appVersion: "1.0"       # version of the app being deployed
