# Test fixtures

Sample XRs that CI renders through every composition. Adding a case here should be
frictionless — that's why `tests/` has the loosest review rule in `CODEOWNERS`.

Name them `xr-<composition-basename>.yaml` so `test-compositions.sh` pairs each with
its composition automatically.
