---
galaxy_info:
  author: linux-course
  description: A simple nginx web server role
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: Ubuntu
      versions: [jammy, noble]

dependencies:
  - role: common      # ensure the base role runs first
