[defaults]
inventory = ./inventory.ini
remote_user = ubuntu
host_key_checking = False
stdout_callback = yaml
# Optionally point at a vault password file (chmod 600) to avoid prompts:
# vault_password_file = ~/.vault_pass
