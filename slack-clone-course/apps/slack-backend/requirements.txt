# The backend's dependencies, grouped by the module that introduces them.
# Install everything with:  pip install -r requirements.txt

# ── Module 02: Django + Postgres ───────────────────────────────────────────────
Django==5.0.*
psycopg[binary]==3.2.*          # Postgres driver
django-environ==0.11.*          # 12-factor settings from env vars

# ── Module 03/04: DRF + auth + API ─────────────────────────────────────────────
djangorestframework==3.15.*
djangorestframework-simplejwt==5.3.*
drf-spectacular==0.27.*         # OpenAPI schema + Swagger UI
django-filter==24.*

# ── Module 05: Channels (WebSockets) ───────────────────────────────────────────
channels==4.1.*
channels-redis==4.2.*           # Redis-backed channel layer
daphne==4.1.*                   # ASGI server (also used by runserver)

# ── Module 06: Redis cache / presence ──────────────────────────────────────────
redis==5.0.*

# ── Module 07: Celery (email, notifications) ───────────────────────────────────
celery==5.4.*
django-celery-beat==2.6.*       # DB-backed periodic-task schedule

# ── Module 08: outgoing webhook delivery ───────────────────────────────────────
requests==2.32.*

# ── Module 11: object-storage uploads ──────────────────────────────────────────
boto3==1.34.*                   # talks to MinIO/S3

# ── Module 12+: production serving ─────────────────────────────────────────────
gunicorn==22.*
uvicorn[standard]==0.30.*
whitenoise==6.*                 # static files in prod

# ── Dev / test ─────────────────────────────────────────────────────────────────
pytest==8.*
pytest-django==4.*
